# Port for the proxy server
PORT=3000

# Target LLM API endpoint (e.g., Claude, local LLM, etc.)
TARGET_LLM_API=http://localhost:11434/api/generate
TARGET_LLM_MODEL=llama2

# Optional: API key for target LLM if required
TARGET_LLM_API_KEY=

# Optional: Custom system prompt for tool conversion
TOOL_SYSTEM_PROMPT=